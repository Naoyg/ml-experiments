# MLflow configuration for local artifact storage
MLFLOW_DEFAULT_ARTIFACT_ROOT=/mlflow/artifacts

# Database configuration (共通)
POSTGRES_DB=mlflow
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow

# Proxy settings (必要に応じて設定)
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=